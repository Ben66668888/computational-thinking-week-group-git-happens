# Station 2
#
# Observed at the station:
#   input  type: str  (looks like a date, e.g. '1990-01-01')
#   output type: str
#
# TODO: replace the sample observations below with what you see at the station,
#       then work out the rule that maps input -> output.

# (input, expected_output) pairs copied from the station
OBSERVATIONS = [
    # ('1990-01-01', '...'),
]


def solution_station_2(station2_input: str) -> str:
    """Decipher the Station 2 algorithm and return its string output."""
    raise NotImplementedError("Station 2 algorithm not deciphered yet")
