# Station 7
#
# Observed at the station:
#   input  type: str  (a formula/expression, e.g. 'a+b', 'd*e', 'e=mc^2')
#   output type: float
#
# TODO: replace the sample observations below with what you see at the station,
#       then work out the rule that maps input -> output.

# (input, expected_output) pairs copied from the station
OBSERVATIONS = [
    # ('a+b', 0.0),
]


def solution_station_7(station7_input: str) -> float:
    """Decipher the Station 7 algorithm and return its float output."""
    raise NotImplementedError("Station 7 algorithm not deciphered yet")
